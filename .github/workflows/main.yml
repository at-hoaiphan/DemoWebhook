name : Backlog Notify
on : push
jobs :
  backlog :
    runs-on : ubuntu-latest
    steps :
      - Name : Backlog Notify
        uses : Bicstone / Backlog-Notify Atto Master
        Env :
          PROJECT_KEY : NOTIFY_GITHUB
          API_HOST : hoaiphan.backlog.com 
          API_KEY : $ {{Secrets.TEST_NOTIFY_GITHUB}}
